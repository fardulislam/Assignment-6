
#### 1) What is the difference between var, let, and const?
1) ans: *var:I can re-declare and re-assign the variable. but let I cannot re-declare in the same scope, but I can re-assign.and cont Cannot re-declare or re-assign the variable.
#### 2) What is the difference between map(), forEach(), and filter()? 
2) ans: map returns a new array . foreach doesn’t return anything and filter can give a filtered array based on a variable 

#### 3) What are arrow functions in ES6?
3)ans:Arrow functions are a new, shorter syntax for writing functions introduced in ES6 . They are more concise than traditional function expressions and have some differences in behavior.

#### 4) How does destructuring assignment work in ES6?
4)ans:It makes my code shorter and more readable.

#### 5) Explain template literals in ES6. How are they different from string concatenation?







